spring:
  main:
    banner-mode: "off"
  application:
    name: shipment-tracking-mcp-server
  datasource:
    url: jdbc:h2:mem:shipmentdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
